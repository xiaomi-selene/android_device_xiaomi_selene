# Device specific properties
import /system/etc/properties/${ro.boot.product.hardware.sku}.prop

# FUSE passthrough
persist.sys.fuse.passthrough.enable=true

# Zygote
zygote.critical_window.minute=10
